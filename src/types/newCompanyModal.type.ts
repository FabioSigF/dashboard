export type NewSchoolModal = {
  nome: string;
  categoria: string;
  cores: string;
  tamanhos: string;
}

export type NewCompanyModal = {
  nome: string;
  cnpj: string;
  segmento: string;
  telefone: string;
  celular: string;
}